services:
  # HTTP API Server - Main API endpoint for playground and direct API calls
  driftlock-http:
    build:
      context: .
      dockerfile: collector-processor/cmd/driftlock-http/Dockerfile
      args:
        - USE_OPENZL=false
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - CORS_ALLOW_ORIGINS=${CORS_ALLOW_ORIGINS:-*}
      - LOG_LEVEL=${LOG_LEVEL:-info}
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - driftlock-network

  # Kafka Collector Processor - Optional, processes OpenTelemetry streams via Kafka
  driftlock-collector:
    build:
      context: .
      dockerfile: collector-processor/cmd/driftlock-collector/Dockerfile
      args:
        - USE_OPENZL=false
    depends_on:
      - kafka
    environment:
      - KAFKA_BROKERS=kafka:29092
      - LOG_LEVEL=${LOG_LEVEL:-info}
    restart: unless-stopped
    networks:
      - driftlock-network
    profiles:
      - kafka  # Only start when explicitly enabled: docker compose --profile kafka up

  # Kafka and Zookeeper - Only needed for collector processor
  zookeeper:
    image: bitnami/zookeeper:3.8
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    ports:
      - "2181:2181"
    networks:
      - driftlock-network
    profiles:
      - kafka

  kafka:
    image: bitnami/kafka:3.7
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - ALLOW_PLAINTEXT_LISTENER=yes
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,PLAINTEXT_INTERNAL://:29092
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://localhost:9092,PLAINTEXT_INTERNAL://kafka:29092
    networks:
      - driftlock-network
    profiles:
      - kafka

networks:
  driftlock-network:
    driver: bridge
