# OpenZL Notes (Archived)

This document has been archived for the YC demo. The default demo build uses standard compressors (zstd/lz4/gzip). OpenZL is optional and disabled by default.

For partners evaluating the demo: no additional libraries are required.
