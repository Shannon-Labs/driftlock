# Driftlock Demo Configuration
# These values are hardcoded in docker-compose.yml for the demo
# For production use, replace with secure values

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=driftlock
DB_USER=postgres
DB_PASSWORD=postgres

# API Configuration
DEFAULT_API_KEY=demo-key-123
DRIFTLOCK_DEV_API_KEY=demo-key-123
DEFAULT_ORG_ID=demo

# OpenTelemetry (optional)
OTEL_SERVICE_NAME=driftlock-api
OTEL_ENV=demo