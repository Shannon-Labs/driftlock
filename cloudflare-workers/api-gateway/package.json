{
  "name": "driftlock-api-gateway",
  "version": "1.0.0",
  "description": "DriftLock API Gateway using Cloudflare Workers",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["cloudflare", "workers", "api", "gateway", "anomaly-detection"],
  "author": "DriftLock Team",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.20.0"
  }
}