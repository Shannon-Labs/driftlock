name = "driftlock-api-gateway"
main = "src/index.ts"
compatibility_date = "2023-10-25"

# Environment variables
[vars]
ENV_SUPABASE_URL = "https://your-project.supabase.co"
ENV_GO_BACKEND_URL = "http://localhost:8081"  # Local dev backend URL

# Account and environment settings
workers_dev = true

# Routes (uncomment and update when ready for production)
# routes = [
#   { pattern = "driftlock.net/*", zone_name = "driftlock.net" }
# ]

# Environment-specific configuration
[env.production]
name = "driftlock-api-gateway-production"