name: Security Scanning

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * 1'  # Run weekly on Mondays

jobs:
  dependency-scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.24'

    - name: Run govulncheck on api-server
      working-directory: api-server
      run: |
        go install golang.org/x/vuln/cmd/govulncheck@latest
        govulncheck ./...

    - name: Run govulncheck on collector-processor
      working-directory: collector-processor
      run: |
        go install golang.org/x/vuln/cmd/govulncheck@latest
        govulncheck ./...

    - name: Run golangci-lint on api-server
      uses: golangci/golangci-lint-action@v3
      with:
        version: latest
        working-directory: api-server

    - name: Run golangci-lint on collector-processor
      uses: golangci/golangci-lint-action@v3
      with:
        version: latest
        working-directory: collector-processor

  frontend-security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install dependencies
      working-directory: web-frontend
      run: npm ci

    - name: Run npm audit
      working-directory: web-frontend
      run: |
        npm audit --audit-level moderate
        # Fail if critical vulnerabilities found
        npm audit --audit-level critical --json | jq -e '.metadata.vulnerabilities.critical > 0 | not'

  secret-scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Run TruffleHog (secret scanning)
      uses: trufflesecurity/truffleHog@main
      with:
        path: ./
        base: ${{ github.event.repository.default_branch }}
        head: HEAD

  container-scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Build Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: false
        load: true
        tags: driftlock-api:scan

    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'driftlock-api:scan'
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v2
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'
