Minimal static stub for the Driftlock UI.

Served by any static file server (or via the Go service if/when you integrate).

For now, this page queries `/healthz` and `/v1/version` to reflect service state.

